#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        int k;
        cin>>k;
        int arr[n];
        for(int i=0;i<n;i++){
            cin>>arr[i];
        }
        unordered_set<int> s;
        for(int i=0;i<n;i++){
            s.insert(arr[i]);
        }
        //check most frequent element
        // int max_freq=0;
        // int max_freq_element=0;
        // for(auto x:mp){
        //     max_freq=max(max_freq,x.second);
        //     if(x.second == max_freq){
        //         max_freq_element=x.first;
        //     }
        // }
        // if(k==max_freq_element){
        //     cout<<"YES"<<endl;
        // }
        // else{
        //     cout<<"NO"<<endl;
        // }
        if(s.find(k)==s.end()){
            cout<<"NO"<<endl;
        }
        else {
            cout<<"YES"<<endl;
        }
    }
}